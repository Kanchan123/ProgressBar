To run the apllication first open command prompt in that folder. And run commands:
    a) npm install
    b) bower install
    c) If you open in development mode then "gulp dev" or in production mode then "gulp".